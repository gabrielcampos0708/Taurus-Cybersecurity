# This is a basic workflow to help you get started with Actions

name: CI

# Controls when the workflow will run
on:
  # Triggers the workflow on push or pull request events but only for the "maingit-init-git-add-.-git-commit--m-\"Primeira-versão-do-site\"-git-branch--M-main-git-remote-add-origin-https/github.com/seuusuario/meu-site.git-git-push--u-origin-main" branch
  push:
    branches: [ "maingit-init-git-add-.-git-commit--m-\"Primeira-versão-do-site\"-git-branch--M-main-git-remote-add-origin-https/github.com/seuusuario/meu-site.git-git-push--u-origin-main" ]
  pull_request:
    branches: [ "maingit-init-git-add-.-git-commit--m-\"Primeira-versão-do-site\"-git-branch--M-main-git-remote-add-origin-https/github.com/seuusuario/meu-site.git-git-push--u-origin-main" ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4

      # Runs a single command using the runners shell
      - name: Run a one-line script
        run: echo Hello, world!

      # Runs a set of commands using the runners shell
      - name: Run a multi-line script
        run: |
          echo Add other actions to build,
          echo test, and deploy your project.
<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <title>Meu Site</title>
  <style>
    body { font-family: Arial, sans-serif; margin: 0; padding: 0; }
    header { background: #333; color: #fff; padding: 1em; text-align: center; }
    nav a { margin: 0 10px; color: #fff; text-decoration: none; }
    main { padding: 2em; }
    footer { background: #333; color: #fff; text-align: center; padding: 1em; }
  </style>
</head>
<body>
  <header>
    <h1>Bem-vindo ao meu site</h1>
    <nav>
      <a href="#">Home</a>
      <a href="#">Sobre</a>
      <a href="#">Contato</a>
    </nav>
  </header>
  <main>
    <h2>Olá!</h2>
    <p>Este é um site simples criado com HTML e CSS.</p>
  </main>
  <footer>
    <p>&copy; 2026 Meu Site</p>
  </footer>
</body>
</html>
